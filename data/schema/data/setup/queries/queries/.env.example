# MongoDB Connection String
MONGODB_URI=mongodb://localhost:27017/ecommerce

# Or use MongoDB Atlas
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ecommerce?retryWrites=true&w=majority

# Node Environment
NODE_ENV=development
  };

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 p-6">
      <div className="max-w-6xl mx-auto">
        {/* Header */}
        <div className="bg-white/10 backdrop-blur-lg rounded-2xl p-8 mb-6 border border-white/20">
          <div className="flex items-center gap-4 mb-4">
            <Database className="w-12 h-12 text-green-400" />
            <div>
              <h1 className="text-3xl font-bold text-white mb-2">
                E-commerce MongoDB Catalog Repository
              </h1>
              <p className="text-gray-300">
                Complete GitHub repository with nested document structure implementation
              </p>
            </div>
          </div>
          
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mt-6">
            <div className="bg-white/5 rounded-lg p-4 border border-white/10">
              <FileText className="w-6 h-6 text-blue-400 mb-2" />
              <div className="text-2xl font-bold text-white">{Object.keys(files).length}</div>
              <div className="text-sm text-gray-400">Files</div>
            </div>
            <div className="bg-white/5 rounded-lg p-4 border border-white/10">
              <Code className="w-6 h-6 text-purple-400 mb-2" />
              <div className="text-2xl font-bold text-white">3</div>
              <div className="text-sm text-gray-400">Sample Products</div>
            </div>
            <div className="bg-white/5 rounded-lg p-4 border border-white/10">
              <Database className="w-6 h-6 text-green-400 mb-2" />
              <div className="text-2xl font-bold text-white">MongoDB</div>
              <div className="text-sm text-gray-400">+ Mongoose ODM</div>
            </div>
          </div>
        </div>

        {/* Files */}
        <div className="space-y-4">
          {Object.entries(files).map(([fileName, content]) => (
            <div key={fileName} className="bg-white/10 backdrop-blur-lg rounded-xl border border-white/20 overflow-hidden">
              <div className="bg-gradient-to-r from-purple-600/20 to-blue-600/20 px-6 py-4 flex items-center justify-between border-b border-white/10">
                <div className="flex items-center gap-3">
                  <FileText className="w-5 h-5 text-blue-400" />
                  <span className="font-mono text-white font-semibold">{fileName}</span>
                </div>
                <button
                  onClick={() => copyToClipboard(content, fileName)}
                  className="flex items-center gap-2 px-4 py-2 bg-white/10 hover:bg-white/20 rounded-lg transition-all duration-200 border border-white/20"
                >
                  {copiedFile === fileName ? (
                    <>
                      <Check className="w-4 h-4 text-green-400" />
                      <span className="text-green-400 text-sm">Copied!</span>
                    </>
                  ) : (
                    <>
                      <Copy className="w-4 h-4 text-gray-300" />
                      <span className="text-gray-300 text-sm">Copy</span>
                    </>
                  )}
                </button>
              </div>
              <pre className="p-6 overflow-x-auto text-sm">
                <code className="text-gray-100 font-mono">{content}</code>
              </pre>
            </div>
          ))}
        </div>

        {/* Instructions */}
        <div className="mt-6 bg-gradient-to-r from-blue-600/20 to-purple-600/20 backdrop-blur-lg rounded-xl p-6 border border-white/20">
          <h2 className="text-xl font-bold text-white mb-4">🚀 Setup Instructions</h2>
          <ol className="space-y-3 text-gray-200">
            <li className="flex gap-3">
              <span className="font-bold text-blue-400">1.</span>
              <span>Create a new directory and copy all files above into their respective locations</span>
            </li>
            <li className="flex gap-3">
              <span className="font-bold text-blue-400">2.</span>
              <span>Run <code className="bg-black/30 px-2 py-1 rounded">npm install</code> to install dependencies</span>
            </li>
            <li className="flex gap-3">
              <span className="font-bold text-blue-400">3.</span>
              <span>Update MongoDB connection string in setup/dbSetup.js</span>
            </li>
            <li className="flex gap-3">
              <span className="font-bold text-blue-400">4.</span>
              <span>Run <code className="bg-black/30 px-2 py-1 rounded">npm run setup</code> to populate the database</span>
            </li>
            <li className="flex gap-3">
              <span className="font-bold text-blue-400">5.</span>
              <span>Initialize git repo and push to GitHub</span>
            </li>
          </ol>
        </div>
      </div>
    </div>
  );
};

export default EcommerceMongoRepo;
